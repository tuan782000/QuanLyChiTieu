import CategoryManagerScreen from '../../screens/CategoryManagerScreen'; // Import màn hình thêm giao dịch

export default CategoryManagerScreen; // Xuất màn hình này làm default export của route (dùng cho expo-router)
